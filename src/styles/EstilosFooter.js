import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    image: {
        width: 40,
        height: 40,
    },
    contenedorFooter: {
        width: '100%',
        alignItems: 'center',
        backgroundColor: '#3aa569',
    },
});